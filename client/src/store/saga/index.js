import {takeEvery, takeLatest} from "redux-saga/effects";
import types from '../actionsTypes'
// import {setEmailInput} from "../saga/sagas";

export function* watchSaga() {
  // yield takeLatest(types.SET_EMAIL_INPUT, setEmailInput);
}
